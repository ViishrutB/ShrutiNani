---
interface Props {
  title: string;
  lede?: string;
}

const { title, lede } = Astro.props;
---

<div class="mb-10">
  <h1 class="text-3xl sm:text-4xl">{title}</h1>
  {lede && <p class="mt-3 max-w-prose text-lg text-muted">{lede}</p>}
</div>
